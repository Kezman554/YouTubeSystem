Progress Log - Content Intelligence System
===========================================

2026-02-15 Session
-------------------
1. Auto-embed transcripts on save
   - Modified Competitive Intel UI (competitive_intel.py) so that saving or updating
     a transcript automatically chunks it and embeds it into LanceDB, making it
     immediately searchable without needing to run a separate script.

2. Deduped LanceDB transcript_chunks table
   - Removed 281 duplicate rows caused by running the batch embed script on
     already-embedded videos. Table went from 562 -> 281 rows.

3. Fixed missing transcript for video f9Ia5vl32qQ
   - Transcript file existed on disk (data/transcripts/) but the SQLite row had
     empty text. Loaded the file content into the database and embedded it.
     Final count: 288 chunks across 34 videos.

4. Competitive Intel UI improvements
   - Copy Video ID and Copy YouTube URL buttons now use one-click clipboard copy
     with "Copied!" confirmation instead of expanding to show the ID.
   - Added "Transcript Available" filter checkbox alongside renamed
     "Missing Transcript" filter on the Videos tab.

5. PDF upload and auto-ingest on Canon Sources (niche_setup.py)
   - Added st.file_uploader for PDFs on the Canon Sources tab.
   - Uploaded PDFs are saved to data/sources/{niche-slug}/.
   - Ingestion (extract, chunk, embed) runs automatically on upload.
   - Wired up the previously stubbed "Trigger Ingestion" and "Re-ingest" buttons
     to the real pipeline.

6. Hide default Streamlit sidebar navigation
   - Added .streamlit/config.toml with showSidebarNavigation = false.
   - Removes the auto-generated page list that linked to blank pages,
     keeping only the custom Navigation menu.

7. Wired up Scrape/Re-scrape buttons on Competitors tab (niche_setup.py)
   - Added _scrape_channel() helper that fetches channel info + videos via
     YouTube Data API, updates channel stats, inserts new videos into DB,
     and marks channel as scraped.
   - Added per-channel scrape controls: max videos dropdown (50/100/200/500/All)
     and "Skip already scraped" checkbox.
   - Both "Scrape Channel" and "Re-scrape" buttons now call the real scraper
     with spinner feedback and error handling.

## 2026-02-19 — Production Context DB Layer

Added 3 new SQLite tables and a full CRUD module for the production context feature:

**Tables added to `schema.py` (now 17 total):**
- `production_context` — single-row: current topic + angle
- `production_canon_passages` — canon passages pinned to the active context (FK → production_context)
- `production_transcript_chunks` — competitor transcript chunks pinned to the active context (FK → production_context)

**New module: `src/database/production_context.py`**
- `get_or_create_production_context()` — returns or creates the single context row
- `update_production_topic(topic, angle)` — set topic/angle on active context
- `add_canon_passage(passage_data)` — pin a canon passage (requires `text`)
- `remove_canon_passage(id)` — unpin by row ID
- `add_transcript_chunk(chunk_data)` — pin a transcript chunk (requires `text`)
- `remove_transcript_chunk(id)` — unpin by row ID
- `get_production_context()` — full context dict with both passage lists
- `clear_production_context()` — delete all pinned items, reset topic/angle
- `get_production_item_count()` — total pinned items (for nav badge)

All functions exported from `src/database/__init__.py`. Smoke-tested end-to-end via Python shell — persistence and all CRUD operations verified.

## 2026-02-19 — Canon Search Result Card Enhancements

Updated `render_canon_result()` in `src/ui/pages/research.py`:
- Added **Authority score** metric (from `canon_sources.priority` field), displayed as "X/10"
- Changed attribution line to **breadcrumb format**: Book > Chapter > p. X
- Layout changed from 2-column to 3-column to fit Authority + Relevance side-by-side
- Existing relevance score display preserved

## 2026-02-19 — Add to Context Buttons on Search Results

Added "Add to Context" buttons to both canon and transcript search result cards in
`src/ui/pages/research.py`:
- Each result card shows a **"+ Add to Context"** button below the preview expander
- Clicking calls `add_canon_passage()` or `add_transcript_chunk()` from production_context module
- Shows **toast notification** with current total item count: "Added to Context (N items)"
- Pages **rerun** after adding so the button state updates immediately
- Already-added items show **"Added ✓" disabled button** — checked by matching LanceDB `id`
  against `chunk_id` in `production_canon_passages` / `production_transcript_chunks`
- Added `_get_added_chunk_ids()` helper that loads all pinned chunk IDs once per render cycle
